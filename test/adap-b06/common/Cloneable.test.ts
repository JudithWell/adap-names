import { it, expect, describe } from 'vitest'
import { Cloneable } from "../../../src/adap-b05/common/Cloneable";

describe('Dummy Test to satisfy github workflow', () => {
    it("still a dummy, don't trust it!", () => {
        expect(true).toBe(true);
    });
});